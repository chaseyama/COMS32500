<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml"
  lang="en-GB" xml:lang="en-GB">
<head>
    <meta charset="UTF-8"/>
    <title>Study Abroad Bristol</title>
    <!-- Bootstrap: Latest compiled and minified CSS -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.0/css/bootstrap.min.css"/>
    <link rel="stylesheet" href="styles/index.css"/>
</head>
    <body>    
        <!--Navigation Bar-->  
        <%- include('partials/navbar') %>

        
        <!--Main Content-->  
        <main class="container">
            <% if(success_message){ %>
                <div class="alert alert-success"><%= success_message %></div>
            <% }%>
            <div class="row">
                <section id="user_profile" class="well col-lg-6 col-md-6 col-sm-6 col-xs-6">
                    
                    <a href="/logout" class="btn btn-secondary">Logout</a>
                </section>
                <section id="Notifications" class="well col-lg-6 col-md-6 col-sm-6 col-xs-6">
                    <h3>Notifications:</h3>
                        <% if(notifications){ %> 
                            <% for(var i=0; i < notifications.length; i++) { %>
                                <div class="well well-sm">
                                    <h4><%= notifications[i].msg %></h4>
                                </div>
                            <%}
                        }else{%>
                            <p>No Notifications</p>
                        <%}%>     
                </section>
            </div>

            <div class="row">
                <h3>Items For Sale:</h3>  
                <section style="overflow-y: scroll; height: 18em;" class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
                <% if(myItems){ %> 
                    <% for(var i=0; i < myItems.length; i++) { %>
                        <div class="well well-sm">
                            <h4><%= myItems[i].itemName %></h4>
                            <p>Asking Price: £<%= myItems[i].price %></p>
                            <p>Description: <%= myItems[i].description %></p>
                            <p>Delete Item:</p>
                            <form action="/delete_items" method="post">
                                <input type="hidden" name="delete" value="<%= myItems[i].id%>"/>
                                <input type="submit" name=""/>
                            </form>
                        </div>
                    <%}
                }else{%>
                    <p>No Items for Sale</p>
                <%}%>     
                </section>
            </div>
            <div class="row">
                <h3>Your Questions:</h3>
                <section></section>
            </div>
            


        </main>
        <footer>
            <p>Jack Allison and Cameron Haseyama</p>
        </footer>
    </body>

    <!-- Bootstrap: jQuery library --> 
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>

    <!-- Bootstrap: Latest compiled JavaScript -->
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.0/js/bootstrap.min.js"></script> 
    
</html>
